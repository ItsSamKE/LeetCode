class Solution:
    def longestCommonPrefix(self, strs: list[str]) -> str:

        prefix = ''
        for word in strs:
            for character in word:
                if len(prefix) > 0:

                else:
                    prefix =
